from setuptools import setup

setup(
    name='OnePackLuck',
    version='0.1',
    packages=['modulo'],
    install_requires=[
        'numpy',
        'matplotlib',
        'astropy'
        # Agrega aquí las dependencias necesarias
    ],
)
